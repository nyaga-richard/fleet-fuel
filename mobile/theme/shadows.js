// Shadows — restrained; cards float slightly, sheets/overlays a bit more.
export const shadowCard = {
  shadowColor: '#000',
  shadowOpacity: 0.22,
  shadowRadius: 8,
  shadowOffset: { width: 0, height: 4 },
  elevation: 2,
};

export const shadowFloat = {
  shadowColor: '#000',
  shadowOpacity: 0.35,
  shadowRadius: 16,
  shadowOffset: { width: 0, height: 8 },
  elevation: 8,
};
